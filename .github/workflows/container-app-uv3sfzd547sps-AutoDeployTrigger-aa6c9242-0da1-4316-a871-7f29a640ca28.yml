name: Trigger auto deployment for container-app-uv3sfzd547sps

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ node-update-base ]
    paths:
    - '**'
    - '.github/workflows/container-app-uv3sfzd547sps-AutoDeployTrigger-aa6c9242-0da1-4316-a871-7f29a640ca28.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPUV3SFZD547SPS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPUV3SFZD547SPS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPUV3SFZD547SPS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistryuv3sfzd547sps.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPUV3SFZD547SPS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPUV3SFZD547SPS_REGISTRY_PASSWORD }}
          containerAppName: container-app-uv3sfzd547sps
          resourceGroup: e2etests96fa
          imageToBuild: containerregistryuv3sfzd547sps.azurecr.io/container-app-uv3sfzd547sps:${{ github.sha }}
          


