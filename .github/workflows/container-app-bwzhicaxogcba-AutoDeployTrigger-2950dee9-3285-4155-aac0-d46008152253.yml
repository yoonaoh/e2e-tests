name: Trigger auto deployment for container-app-bwzhicaxogcba

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ node-update-base ]
    paths:
    - '**'
    - '.github/workflows/container-app-bwzhicaxogcba-AutoDeployTrigger-2950dee9-3285-4155-aac0-d46008152253.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPBWZHICAXOGCBA_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPBWZHICAXOGCBA_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPBWZHICAXOGCBA_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistrybwzhicaxogcba.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPBWZHICAXOGCBA_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPBWZHICAXOGCBA_REGISTRY_PASSWORD }}
          containerAppName: container-app-bwzhicaxogcba
          resourceGroup: e2etests9d2d
          imageToBuild: containerregistrybwzhicaxogcba.azurecr.io/container-app-bwzhicaxogcba:${{ github.sha }}
          


