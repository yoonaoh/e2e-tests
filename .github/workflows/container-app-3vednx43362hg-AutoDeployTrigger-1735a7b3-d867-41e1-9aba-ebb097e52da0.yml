name: Trigger auto deployment for container-app-3vednx43362hg

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ node-create-base ]
    paths:
    - 'api/**'
    - '.github/workflows/container-app-3vednx43362hg-AutoDeployTrigger-1735a7b3-d867-41e1-9aba-ebb097e52da0.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPP3VEDNX43362HG_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP3VEDNX43362HG_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP3VEDNX43362HG_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/api 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPP3VEDNX43362HG_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP3VEDNX43362HG_REGISTRY_PASSWORD }}
          containerAppName: container-app-3vednx43362hg
          resourceGroup: e2etestsb094
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


