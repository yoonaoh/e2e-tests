name: Trigger auto deployment for container-app-4dcozmrhuoks2

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ node-update-base ]
    paths:
    - '**'
    - '.github/workflows/container-app-4dcozmrhuoks2-AutoDeployTrigger-e6676423-f1fc-4f87-9bd8-8bab85a8e42f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPP4DCOZMRHUOKS2_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP4DCOZMRHUOKS2_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP4DCOZMRHUOKS2_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistry4dcozmrhuoks2.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPP4DCOZMRHUOKS2_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP4DCOZMRHUOKS2_REGISTRY_PASSWORD }}
          containerAppName: container-app-4dcozmrhuoks2
          resourceGroup: e2etests9f1b
          imageToBuild: containerregistry4dcozmrhuoks2.azurecr.io/container-app-4dcozmrhuoks2:${{ github.sha }}
          


